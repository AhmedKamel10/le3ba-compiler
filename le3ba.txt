int n = 4;
int arr = alloc(n);
set(arr, 0, 10);
set(arr, 1, 55);
set(arr, 2, 22);
set(arr, 3, 40);

int target = 55;
int found = 0;
int k = 0;
int current = 0;
while (k < 4) {
    current = get(arr, k);
    if (current == target) {
        found = 1;
    };
    k = k + 1;
};
int result = found;
print result;
